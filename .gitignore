/.cpcache/
/.nbb/
/node_modules/
/target/

# Real statement exports and real ingest output are personal financial data.
# Only fixtures under test/ and the institution mappings under resources/ —
# neither of which contains an account number, a balance or a payee — belong
# in this repository.
/data/
/out/
*.statement.edn
*.ledger.edn
